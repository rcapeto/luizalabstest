import { FunctionComponent } from 'react';

import styles from './styles.module.scss';

export const PaginationDot: FunctionComponent = () => {
   return(
      <p className={styles.paginationDot}>..</p>
   );
}